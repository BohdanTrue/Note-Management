export enum ErrorMessage {
  TitleLess = `Note title can't be less than 4 symbols`,
  TitleMore = `Note title can't be more than 60 symbols`,
  NoteExists = `Note with this title already exists`,
  NoNotes = `We can't load notes`,
  None = '',
}